apiVersion: apps/v1
kind: Deployment
metadata:
    name: mongodb-test
spec:
    selector:
        matchLabels:
            app: mongodb
    replicas: 1
    template:
        metadata:
            labels:
                app: mongodb
        spec:
            containers:
                - name: mongodb
                  imagePullPolicy: Never
                  image: mongodb:test
                  resources:
                      limits:
                          cpu: 50m
                          memory: 200Mi
                      requests:
                          cpu: 25m
                          memory: 20Mi
                  ports:
                      - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
    name: mongodb-service
spec:
    type: NodePort
    selector:
        app: mongodb
    ports:
        - name: mongodb
          protocol: TCP
          port: 27017
          targetPort: 27017
          nodePort: 30017
